_administration_realmproduction, this file should be placed under:
# TOMCAT/shared/classes/shared-config.properties

# key: {realmId}.auth.salt
# value: must be the same as the security.token cluster property from SP0001
#        so that the cookies generated by SP0001 will be treated as valid by SP0012
# for example: administration_realm.auth.salt=111
global.auth.salt=30971765174504

test_realm.auth.salt=1802305133829

pp.forgotpasswordurl=http://localhost:8181/retention-portal/#/resetpassword/
pp.loginurl=http://localhost:8181/retention-portal/#/login/
pp.getpatientinfourl=http://localhost:8181/retention-portal/#/getpatientinfo
ctsUrl=http://localhost:8181/cts

trialDirect.cookie.isSecured=false
